/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "avatars.githubusercontent.com",
      "source.unsplash.com",
      "image.tmdb.org",
    ],
  },
};

module.exports = nextConfig;
